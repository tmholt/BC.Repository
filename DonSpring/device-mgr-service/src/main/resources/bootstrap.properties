## bootstrap.properties is read earlier than application.properties.
## we want to load config early (from config service)

spring.application.name=don.service.device-mgr
spring.cloud.config.uri=http://localhost:8888